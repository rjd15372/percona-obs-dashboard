{
  "planPath": "docs/superpowers/plans/2026-07-10-obs-traffic-reduction.md",
  "tasks": [
    {
      "id": 7,
      "subject": "Task 1: Env type and Task interface thread-through",
      "status": "pending",
      "description": "**Goal:** Introduce `obs.Env` and thread an `env *obs.Env` parameter through the `Task` interface, all task implementations, `ProcessOnce`, and every test call site — behavior unchanged (env ignored everywhere for now).\n\n**Files:**\n- Create: `backend/internal/obs/env.go`\n- Modify: `backend/internal/worker/worker.go`\n- Modify: `backend/internal/obs/tasks.go` (all 8 Run methods)\n- Test: `backend/internal/obs/tasks_test.go`, `backend/internal/worker/worker_test.go`\n\n**Acceptance Criteria:**\n- obs.Env exists with BuildStates and RepoStates fields\n- worker.Task interface is Run(ctx, client, pkg, env *obs.Env) error\n- All existing tests pass unchanged in behavior\n\n**Verify:** `cd backend && go build ./... && go test ./...`\n\n**Steps:** Full code in plan Task 1. Commit with `git commit -s` (no Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"backend/internal/obs/env.go\", \"backend/internal/obs/tasks.go\", \"backend/internal/worker/worker.go\", \"backend/internal/obs/tasks_test.go\", \"backend/internal/worker/worker_test.go\"], \"verifyCommand\": \"cd backend && go build ./... && go test ./...\", \"acceptanceCriteria\": [\"obs.Env exists with BuildStates and RepoStates fields\", \"Task interface carries env *obs.Env\", \"all existing tests pass\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 8,
      "subject": "Task 2: BuildStateTask, PublishStateTask, BinariesCheckTask consume Env",
      "status": "pending",
      "blockedBy": [7],
      "description": "**Goal:** The three tasks that fetch `_result` data use pre-fetched Env data when present, skipping their own OBS calls.\n\n**Files:**\n- Modify: `backend/internal/obs/tasks.go`\n- Test: `backend/internal/obs/tasks_test.go`\n\n**Acceptance Criteria:**\n- With env.BuildStates set, BuildStateTask makes zero HTTP requests and applies the given states\n- With env.RepoStates set, PublishStateTask and BinariesCheckTask make zero _result?view=status requests\n- With env == nil, all three behave exactly as before\n\n**Verify:** `cd backend && go test ./internal/obs/ -v`\n\n**Steps:** TDD; full test code and implementation in plan Task 2. Commit with `git commit -s`.\n\n```json:metadata\n{\"files\": [\"backend/internal/obs/tasks.go\", \"backend/internal/obs/tasks_test.go\"], \"verifyCommand\": \"cd backend && go test ./internal/obs/ -v\", \"acceptanceCriteria\": [\"BuildStateTask uses env.BuildStates without HTTP\", \"PublishStateTask/BinariesCheckTask use env.RepoStates without HTTP\", \"nil env preserves existing behavior\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 9,
      "subject": "Task 3: Config keys for backoff, batching, and rate budget",
      "status": "pending",
      "description": "**Goal:** Add `worker_pool.backoff_max`, `worker_pool.batch_threshold`, and `obs.minute_request_budget` config keys with defaults and env bindings; document them in both example files.\n\n**Files:**\n- Modify: `backend/internal/config/config.go`\n- Modify: `backend/config.yaml.example`, `config.yaml.example`\n- Test: `backend/internal/config/config_test.go`\n\n**Acceptance Criteria:**\n- Defaults: BackoffMax=5m, BatchThreshold=4, MinuteRequestBudget=60\n- Env overrides: WORKER_POOL_BACKOFF_MAX, WORKER_POOL_BATCH_THRESHOLD, OBS_MINUTE_REQUEST_BUDGET\n- Both example YAML files document the new keys\n\n**Verify:** `cd backend && go test ./internal/config/ -v`\n\n**Steps:** TDD; full code in plan Task 3. Root config.yaml.example has no worker_pool section yet — add the whole block. Commit with `git commit -s`.\n\n```json:metadata\n{\"files\": [\"backend/internal/config/config.go\", \"backend/internal/config/config_test.go\", \"backend/config.yaml.example\", \"config.yaml.example\"], \"verifyCommand\": \"cd backend && go test ./internal/config/ -v\", \"acceptanceCriteria\": [\"defaults 5m/4/60\", \"env overrides bound\", \"example YAMLs document keys\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 10,
      "subject": "Task 4: Per-minute rate limiter with interactive bypass",
      "status": "pending",
      "blockedBy": [9],
      "description": "**Goal:** Background OBS requests pass through a fixed-window per-minute budget in the client; API-handler requests bypass it via a context tag; limiter stats surface in MetricsSnapshot().\n\n**Files:**\n- Create: `backend/internal/obs/interactive.go`, `backend/internal/obs/limiter.go`, `backend/internal/obs/limiter_test.go`\n- Modify: `backend/internal/obs/client.go`, `backend/internal/api/server.go`, `backend/cmd/obsboard/main.go`\n\n**Acceptance Criteria:**\n- Budget exhausted → background acquire blocks; cancelled context unblocks with ctx.Err()\n- Window rollover resets the budget\n- obs.Interactive(ctx) requests skip the limiter entirely\n- Budget <= 0 disables limiting (NewClient defaults to 0 so existing tests unaffected)\n- MetricsSnapshot() includes limiter_waits and limiter_remaining when enabled\n\n**Verify:** `cd backend && go build ./... && go test ./internal/obs/ -v`\n\n**Steps:** TDD; full code in plan Task 4. limiter_test.go is an internal test (package obs) using injectable clock and cancelled-context assertions — no real sleeps. Commit with `git commit -s`.\n\n```json:metadata\n{\"files\": [\"backend/internal/obs/interactive.go\", \"backend/internal/obs/limiter.go\", \"backend/internal/obs/limiter_test.go\", \"backend/internal/obs/client.go\", \"backend/internal/api/server.go\", \"backend/cmd/obsboard/main.go\"], \"verifyCommand\": \"cd backend && go build ./... && go test ./internal/obs/ -v\", \"acceptanceCriteria\": [\"exhausted budget blocks background requests, ctx cancel unblocks\", \"window rollover resets budget\", \"Interactive ctx bypasses limiter\", \"budget<=0 disables limiting\", \"MetricsSnapshot exposes limiter_waits/limiter_remaining\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 11,
      "subject": "Task 5: Adaptive backoff in the working set",
      "status": "pending",
      "blockedBy": [7, 9],
      "description": "**Goal:** Working-set entries carry {interval, nextDue}; the scheduler dispatches only due packages; unchanged passes double the interval (30s → 5m cap), changed passes and wake signals reset it.\n\n**Files:**\n- Modify: `backend/internal/workingset/workingset.go` (full rework, complete file in plan)\n- Modify: `backend/internal/worker/worker.go` (ProcessOnce returns changed bool; targetStatesChanged helper)\n- Modify: `backend/cmd/obsboard/main.go`\n- Test: `backend/internal/workingset/workingset_test.go`, `backend/internal/worker/worker_test.go`\n\n**Acceptance Criteria:**\n- Done(key, changed=false) doubles the interval up to backoffMax; changed=true resets to base\n- DispatchDue() dispatches only entries with nextDue <= now and not inflight\n- Signal resets the schedule and dispatches immediately; Add on existing resets schedule without immediate dispatch\n- ProcessOnce returns true iff rollup state or any target's build state changed\n\n**Verify:** `cd backend && go test ./internal/workingset/ ./internal/worker/ -v`\n\n**Steps:** TDD; full code in plan Task 5, including fakeClock helpers and ladder tests. Mechanical fallout: workingset.New(10) call sites gain (10, 30*time.Second, 5*time.Minute, 4); ws.Done(key) gains `, false`. Commit with `git commit -s`.\n\n```json:metadata\n{\"files\": [\"backend/internal/workingset/workingset.go\", \"backend/internal/workingset/workingset_test.go\", \"backend/internal/worker/worker.go\", \"backend/internal/worker/worker_test.go\", \"backend/cmd/obsboard/main.go\"], \"verifyCommand\": \"cd backend && go test ./internal/workingset/ ./internal/worker/ -v\", \"acceptanceCriteria\": [\"unchanged Done doubles interval up to cap\", \"changed Done resets to base\", \"DispatchDue only dispatches due entries\", \"Signal resets and dispatches immediately\", \"ProcessOnce returns changed flag\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 12,
      "subject": "Task 6: Threshold-based project-level batch dispatch",
      "status": "pending",
      "blockedBy": [8, 11],
      "description": "**Goal:** The dispatch channel carries Jobs; at each tick, due packages of the same project >= threshold become one batch job whose worker makes a single project-level _result call and injects it into every package's pass.\n\n**Files:**\n- Modify: `backend/internal/workingset/workingset.go` (Job type, chan Job, grouping, sendJob)\n- Modify: `backend/internal/worker/worker.go` (run loop → exported ProcessJob)\n- Modify: `backend/internal/obs/client.go` (BuildResults gains Details: s.Details — parity fix)\n- Test: `backend/internal/workingset/workingset_test.go`, `backend/internal/worker/worker_test.go`\n\n**Acceptance Criteria:**\n- DispatchDue groups due packages by project; groups >= threshold become one Job{ProjectFetch: true}; smaller groups become single-package jobs\n- Batch worker makes exactly one _result request for the whole job and every package's states are applied\n- Project fetch failure → every package in the job falls back to per-package fetches\n- Add/Signal immediate dispatches are single-package jobs\n- BuildResults carries per-status Details\n\n**Verify:** `cd backend && go test ./internal/workingset/ ./internal/worker/ -v`\n\n**Steps:** TDD; full code in plan Task 6, including httptest servers that discriminate project-level (bare _result) vs per-package (_result?package=...) requests. Commit with `git commit -s`.\n\n```json:metadata\n{\"files\": [\"backend/internal/workingset/workingset.go\", \"backend/internal/workingset/workingset_test.go\", \"backend/internal/worker/worker.go\", \"backend/internal/worker/worker_test.go\", \"backend/internal/obs/client.go\"], \"verifyCommand\": \"cd backend && go test ./internal/workingset/ ./internal/worker/ -v\", \"acceptanceCriteria\": [\"due packages grouped by project with threshold\", \"batch job makes exactly one _result call\", \"fetch failure falls back per package\", \"Add/Signal dispatch single-package jobs\", \"BuildResults carries status Details\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-10T12:00:00Z"
}
