{
  "planPath": "docs/superpowers/plans/2026-07-20-visibility-heartbeat.md",
  "tasks": [
    {
      "id": 1,
      "nativeTaskId": 66,
      "subject": "Task 1: heartbeat-driven presence gate",
      "status": "pending",
      "description": "**Goal:** `presence.Gate` is heartbeat-driven (`Heartbeat()`/`State()`, no `Connect`/`Disconnect`); the hub's `Presence` hook is removed; `main.go` no longer wires it.\n\n**Files:**\n- Modify: `backend/internal/presence/presence.go` (whole file — complete replacement in the plan)\n- Modify: `backend/internal/presence/presence_test.go` (whole file — complete replacement in the plan)\n- Modify: `backend/internal/hub/hub.go` (remove Presence interface/field/calls)\n- Modify: `backend/internal/hub/hub_test.go` (delete `stubPresence` + `TestPresenceHook`)\n- Modify: `backend/cmd/obsboard/main.go` (delete the `h.Presence = gate` line only; `gate := presence.New(...)` stays)\n\n**Acceptance Criteria:**\n- [ ] `Gate.Heartbeat()` stamps `lastSeen` and signals subscribers exactly once per idle→active transition; `Active()` = `now − lastSeen < linger`; boot-seeded `lastSeen` keeps the boot-grace behavior\n- [ ] `State()` returns `\"active\"`/`\"idle\"` (\"active\" always when disabled); disabled gate: `Heartbeat()` no-op, never signals\n- [ ] `grep -rn \"Connect\\|Disconnect\" internal/presence/ internal/hub/` → no matches; `hub.Hub` has no `Presence` field\n- [ ] `go build ./... && go test ./internal/presence/ ./internal/hub/ -count=1` pass\n\n**Verify:** `cd backend && go test ./internal/presence/ ./internal/hub/ -count=1 -v && go build ./...` → all PASS, build OK\n\n**Steps:** TDD per plan Task 1 (full code in the plan): replace presence_test.go first (boot grace, heartbeat keeps-active-then-expires, wake once per transition, State, disabled); then replace presence.go (Heartbeat/State/lastSeen); strip the hub Presence hook + its test; delete main.go's `h.Presence = gate`. Commit `git commit -s -m \"feat(presence): heartbeat-driven gate; drop SSE connection counting\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"backend/internal/presence/presence.go\", \"backend/internal/presence/presence_test.go\", \"backend/internal/hub/hub.go\", \"backend/internal/hub/hub_test.go\", \"backend/cmd/obsboard/main.go\"], \"verifyCommand\": \"cd backend && go test ./internal/presence/ ./internal/hub/ -count=1 -v && go build ./...\", \"acceptanceCriteria\": [\"heartbeat recency drives Active with boot grace\", \"State active/idle incl. disabled semantics\", \"Connect/Disconnect fully removed from presence+hub\", \"suite + build green\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "nativeTaskId": 67,
      "subject": "Task 2: POST /api/presence + polling state in metrics",
      "status": "pending",
      "blockedBy": [1],
      "description": "**Goal:** The router accepts heartbeats and the metrics payload reports the gate's decision.\n\n**Files:**\n- Create: `backend/internal/api/presence.go`\n- Create: `backend/internal/api/presence_test.go`\n- Modify: `backend/internal/api/metrics.go` (PollState interface, response field, handler param, doc comment)\n- Modify: `backend/internal/api/metrics_test.go` (stub + assertions)\n- Modify: `backend/internal/api/server.go` (PresenceGate param, two routes)\n- Modify: `backend/cmd/obsboard/main.go` (NewRouter call passes `gate`)\n\n**Acceptance Criteria:**\n- [ ] `POST /api/presence` → 204 and exactly one `Heartbeat()` call on the gate\n- [ ] `/api/metrics` JSON has top-level `polling` equal to the gate's `State()` (stub returns \"active\" in the test)\n- [ ] `go test ./internal/api/ -count=1` passes; `go build ./...` OK\n\n**Verify:** `cd backend && go test ./internal/api/ -run 'TestPresenceHandler|TestMetricsHandler' -count=1 -v` → all PASS\n\n**Steps:** TDD per plan Task 2 (full code in the plan): failing tests first (spyBeater 204 test; fakePollState + 5-arg metricsHandler invocation + Polling/raw-key assertions); then api/presence.go (Beater + presenceHandler), metrics.go (PollState, Polling field, gate param, doc comment), server.go (PresenceGate composite interface, NewRouter gains `gate PresenceGate`, `r.Post(\"/api/presence\", …)`, metrics route passes gate), main.go NewRouter call appends `gate`. Commit `git commit -s -m \"feat(api): presence heartbeat endpoint and polling state in /api/metrics\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"backend/internal/api/presence.go\", \"backend/internal/api/presence_test.go\", \"backend/internal/api/metrics.go\", \"backend/internal/api/metrics_test.go\", \"backend/internal/api/server.go\", \"backend/cmd/obsboard/main.go\"], \"verifyCommand\": \"cd backend && go test ./internal/api/ -run 'TestPresenceHandler|TestMetricsHandler' -count=1 -v\", \"acceptanceCriteria\": [\"POST /api/presence 204 + one Heartbeat call\", \"polling key in metrics JSON from gate State\", \"suite + build green\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "nativeTaskId": 68,
      "subject": "Task 3: frontend heartbeat + panel polling state",
      "status": "pending",
      "blockedBy": [2],
      "description": "**Goal:** Dashboard tabs beat every 60s while visible on Builds/Artifacts; the metrics panel header shows `polling active|idle`.\n\n**Files:**\n- Create: `frontend/src/composables/usePresenceHeartbeat.ts`\n- Modify: `frontend/src/App.vue` (import + one call)\n- Modify: `frontend/src/types/metrics.ts` (one field)\n- Modify: `frontend/src/components/MetricsPanel.vue` (header span)\n\n**Acceptance Criteria:**\n- [ ] Composable beats on mount, every 60s, on `visibilitychange`→visible, and on eligibility flipping true — always guarded by `document.visibilityState === 'visible' && eligible.value`; interval/listener/watcher cleaned up on unmount\n- [ ] `App.vue` wires it with `computed(() => mainTab.value !== 'overview')`\n- [ ] `MetricsSnapshot` has `polling: string`; header reads `N connections · polling active · up … · updated …`, with the polling segment absent when the field is empty/missing\n- [ ] `npm run build` exits 0\n\n**Verify:** `cd frontend && npm run build` → exit 0\n\n**Steps:** Per plan Task 3 (full code in the plan): create usePresenceHeartbeat.ts (shouldBeat guard, beat with swallowed errors, 60s interval, visibilitychange listener, eligible watcher, full cleanup); wire in App.vue below the mainTab declaration; add `polling: string` to types; insert the `<template v-if=\"data.polling\"> · polling {{ data.polling }}</template>` segment in the panel header span. Commit `git commit -s -m \"feat(frontend): visibility heartbeats from Builds/Artifacts and polling state in metrics panel\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"frontend/src/composables/usePresenceHeartbeat.ts\", \"frontend/src/App.vue\", \"frontend/src/types/metrics.ts\", \"frontend/src/components/MetricsPanel.vue\"], \"verifyCommand\": \"cd frontend && npm run build\", \"acceptanceCriteria\": [\"guarded 60s beats + immediate beats on visible/eligible\", \"App.vue eligibility = mainTab !== overview\", \"polling segment in header with old-backend tolerance\", \"build exit 0\"], \"modelTier\": \"mechanical\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-20T00:00:00Z"
}
