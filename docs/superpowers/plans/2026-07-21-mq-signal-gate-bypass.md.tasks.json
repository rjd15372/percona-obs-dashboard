{
  "planPath": "docs/superpowers/plans/2026-07-21-mq-signal-gate-bypass.md",
  "tasks": [
    {
      "id": 1,
      "nativeTaskId": 69,
      "subject": "Task 1: Signal bypasses the idle gate",
      "status": "completed",
      "description": "**Goal:** `Signal()` dispatches while the gate is idle; `Add`/`DispatchDue` keep today's gating; comments and example configs describe the new behavior.\n\n**Files:**\n- Modify: `backend/internal/workingset/workingset.go` (split sendJob, reroute Signal, doc comments)\n- Modify: `backend/internal/workingset/workingset_test.go` (append one test)\n- Modify: `backend/internal/mq/consumer.go` (one comment line)\n- Modify: `config.yaml.example` + `backend/config.yaml.example` (idle block comment)\n\n**Acceptance Criteria:**\n- [ ] With an idle stub gate: `Signal` puts the job on `Dispatch()` immediately; a second `Signal` on the now-in-flight package does NOT double-dispatch\n- [ ] Existing `TestGateBlocksDispatchWhileIdle` still passes unchanged (Add + scheduler stay gated, wake drains)\n- [ ] `mqStateToRollup`'s comment says \"the signaled worker pass\"; both example configs say MQ build events still trigger targeted per-package fetches while idle\n- [ ] `go test ./... -count=1 && go build ./...` pass\n\n**Verify:** `cd backend && go test ./internal/workingset/ -run 'TestSignalBypassesIdleGate|TestGateBlocksDispatchWhileIdle' -count=1 -v` \u2192 all PASS\n\n**Steps:** TDD per plan Task 1 (full code in the plan): failing `TestSignalBypassesIdleGate` first (reuses existing stubGate/pkg helpers); then split `sendJob` into ungated `enqueue` + gated `sendJob` wrapper, `Signal` calls `enqueue` with the extended doc comment; consumer comment word-swap; both example-config idle blocks replaced. Commit `git commit -s -m \"feat(workingset): MQ-signaled jobs bypass the idle gate\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"backend/internal/workingset/workingset.go\", \"backend/internal/workingset/workingset_test.go\", \"backend/internal/mq/consumer.go\", \"config.yaml.example\", \"backend/config.yaml.example\"], \"verifyCommand\": \"cd backend && go test ./internal/workingset/ -run 'TestSignalBypassesIdleGate|TestGateBlocksDispatchWhileIdle' -count=1 -v\", \"acceptanceCriteria\": [\"idle Signal dispatches, in-flight Signal doesn't double-dispatch\", \"Add/DispatchDue stay gated (existing test green)\", \"consumer + example-config comments updated\", \"suite + build green\"], \"modelTier\": \"mechanical\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-21T11:02:33.783636+00:00"
}