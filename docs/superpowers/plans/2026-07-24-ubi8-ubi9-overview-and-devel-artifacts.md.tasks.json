{
  "planPath": "docs/superpowers/plans/2026-07-24-ubi8-ubi9-overview-and-devel-artifacts.md",
  "tasks": [
    {
      "id": 1,
      "nativeTaskId": 78,
      "subject": "Task 1: Overview CVE aggregation keyed by repo",
      "status": "completed",
      "description": "**Goal:** `buildOverviewSnapshot` produces one `OverviewImage` per `(project, package, repo)` with `Repo` + `BaseOS`; store queries carry `repo`.\n\n**Files:**\n- Modify: `backend/internal/store/overview.go` (`OverviewCveScan.Repo`, `OverviewCvePeriod.Repo`, both queries)\n- Modify: `backend/internal/api/overview.go` (`OverviewImage` fields, `imgKey`, aggregation)\n- Modify: `backend/internal/api/overview_test.go` (new split-by-repo test)\n\n**Acceptance Criteria:**\n- [ ] Two scan rows (same project+package `isv:percona:ppg:staging:17:containers`/`pdp`, repos `ubi8`/`ubi9`, different counts) \u2192 two `OverviewImage`s with distinct `Repo` (\"ubi8\"/\"ubi9\") and `BaseOS` (\"UBI 8\"/\"UBI 9\") and independent Critical/High/OldestOpenDays\n- [ ] Existing `TestOverviewSnapshotBuilder` still passes (scans with empty `Repo` \u2192 one image, `BaseOS` from project fallback)\n- [ ] `QueryAllCveScans`/`QueryAllCvePeriods` select and scan `repo`\n- [ ] `go test ./... -count=1 && go build ./...` pass\n\n**Verify:** `cd backend && go test ./internal/api/ ./internal/store/ -count=1 -v && go build ./...` \u2192 all PASS\n\n**Steps:** TDD per plan Task 1 (full code in the plan): failing `TestOverviewSnapshotSplitsByRepo` first (collect all pdp images across projects, assert 2 with distinct Repo/BaseOS/counts); then add `Repo` to `OverviewCveScan`/`OverviewCvePeriod` + select/scan it in `QueryAllCveScans`/`QueryAllCvePeriods`; add `Repo`/`BaseOS` to `OverviewImage`; change `imgKey` to `{project,pkg,repo}`, create image with `Repo`+`BaseOS: deriveBaseOS(s.Project, s.Repo)` (helper already in release_artifacts.go), key periods + final assembly map by repo too. Commit `git commit -s -m \"feat(overview): key CVE-exposure images by build repo (ubi8/ubi9)\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"backend/internal/store/overview.go\", \"backend/internal/api/overview.go\", \"backend/internal/api/overview_test.go\"], \"verifyCommand\": \"cd backend && go test ./internal/api/ ./internal/store/ -count=1 -v && go build ./...\", \"acceptanceCriteria\": [\"two repos same package -> two images distinct Repo/BaseOS/counts\", \"existing builder test still green (empty repo -> one image, project-fallback base OS)\", \"both overview queries carry repo\", \"suite + build green\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "nativeTaskId": 79,
      "subject": "Task 2: Overview CVE table repo\u00b7base-OS rows (frontend)",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** The exposure table labels each row `name \u00b7 base_os`, expands each repo independently, and shows only that repo's findings.\n\n**Files:**\n- Modify: `frontend/src/types/overview.ts` (`OverviewImage` fields)\n- Modify: `frontend/src/components/CveExposureTable.vue` (label, imgKey, report filter; remove `imageSuffix`)\n\n**Acceptance Criteria:**\n- [ ] `OverviewImage` type has `repo: string` and `base_os: string`\n- [ ] Rows render `{{ img.name }} \u00b7 {{ img.base_os }}`; `imageSuffix` and its usage are gone\n- [ ] `imgKey` is `project + '/' + name + '/' + repo`; the report fetch filters returned scans to `s.repo === img.repo`\n- [ ] `npm run build` exits 0\n\n**Verify:** `cd frontend && npm run build` \u2192 exit 0\n\n**Steps:** Per plan Task 2 (full code in the plan): add `repo`/`base_os` to the `OverviewImage` TS type; delete `imageSuffix` fn + its template sub-span, render `\u00b7 {{ img.base_os }}`; change `imgKey` (and toggleReport/fetchReport param types) to include repo; in fetchReport cache `scans.filter(s => s.repo === img.repo)`. Commit `git commit -s -m \"feat(overview-ui): label CVE rows by base OS, filter report per repo\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"frontend/src/types/overview.ts\", \"frontend/src/components/CveExposureTable.vue\"], \"verifyCommand\": \"cd frontend && npm run build\", \"acceptanceCriteria\": [\"OverviewImage type gains repo + base_os\", \"rows show name \u00b7 base_os, imageSuffix removed\", \"imgKey + report filtered per repo\", \"build exit 0\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 3,
      "nativeTaskId": 80,
      "subject": "Task 3: devel artifacts container fan-out per repo (frontend)",
      "status": "completed",
      "description": "**Goal:** `useArtifacts.ts` emits one `ContainerImage` per distinct target repo, with base OS + registry derived from the repo and CVE scans filtered to it.\n\n**Files:**\n- Modify: `frontend/src/composables/useArtifacts.ts` (`deriveBaseOs` signature, `baseOsFromRepo`, `containerImages` fan-out)\n\n**Acceptance Criteria:**\n- [ ] `deriveBaseOs(project, repo?)` returns the repo's base OS when known, else the project-name fallback\n- [ ] A container package with targets in repos `ubi8` and `ubi9` yields two `ContainerImage`s: distinct `baseOs` (\"UBI 8\"/\"UBI 9\"), `registry` (`\u2026/containers/ubi8/<name>` and `\u2026/ubi9/<name>`), `id` includes repo, `cveScans` filtered to that repo\n- [ ] A container package whose targets are a single repo (`images`, old layout) yields one image, base OS from project fallback, registry `\u2026/images/<name>` (unchanged)\n- [ ] `npm run build` exits 0\n\n**Verify:** `cd frontend && npm run build` \u2192 exit 0\n\n**Steps:** Per plan Task 3 (full code in the plan): rework `deriveBaseOs(project, repo?)` repo-first with `baseOsFromRepo` helper (repo optional so one-arg callers still compile); change `containerImages` from `.map` to `.flatMap` fanning out over distinct `pkg.targets[].repo` (fallback `['images']` when no targets), per repo deriving baseOs/registry (`.../<repo>/<name>`)/pullCmd/published and filtering `cveScans` to `s.repo === repo`, id includes repo. `grep deriveBaseOs(` to confirm no caller broke. Commit `git commit -s -m \"fix(artifacts): fan out devel container images per base-image repo\"` (never Co-Authored-By).\n\n```json:metadata\n{\"files\": [\"frontend/src/composables/useArtifacts.ts\"], \"verifyCommand\": \"cd frontend && npm run build\", \"acceptanceCriteria\": [\"deriveBaseOs repo-first with project fallback\", \"package with ubi8+ubi9 targets -> two images, distinct baseOs/registry, cveScans filtered\", \"single images-repo package -> one image unchanged\", \"build exit 0\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-24T11:03:04.209441+00:00"
}